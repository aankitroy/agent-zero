services:
  agent-zero:
    container_name: agent-zero
    build:
      context: ../../
      dockerfile: docker/run/Dockerfile
    volumes:
      - /Users/aankitroy/Workspace/learnit/agent-zero:/a0
    ports:
      - "50080:80"
